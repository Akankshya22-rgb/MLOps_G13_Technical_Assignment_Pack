# Copy to .env and adjust as needed. docker-compose.yml reads these for local overrides;
# defaults below match the checked-in compose file.

POSTGRES_USER=mlops
POSTGRES_PASSWORD=mlops
POSTGRES_DB=mlops

DATABASE_URL=postgresql://mlops:mlops@db:5432/mlops
CORS_ORIGINS=http://localhost:4200
LOG_LEVEL=INFO
